{
  "name": "pentris",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://www.pentris.net/v3",
  "dependencies": {
    "gh-pages": "^2.0.1",
    "immer": "^2.1.5",
    "prando": "^5.1.0",
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-scripts": "^3.0.1",
    "rxjs": "^6.4.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "postbuild": "cp -r v2site build2 && mv build build2/v3 && mv build2 build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "deploy-dev": "gh-pages -d build -o origin && echo 'www.pentris.net' > build/CNAME && gh-pages -d build -o prod -b master",
    "local": "serve build/"
  },
  "devDependencies": {
    "@types/jest": "^24.0.13",
    "@types/node": "^11.13.10",
    "@types/react": "^16.8.17",
    "@types/react-dom": "^16.8.4",
    "typescript": "^3.4.5"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
